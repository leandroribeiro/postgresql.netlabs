using System.Collections.Generic;
using Postgre.Domain.Entities;

namespace Postgre.Domain.Repositories
{
    public interface IRepository<T> where T : BaseEntity
    {
        void Add(T item);
        void Remove(int id);
        void Update(T item);
        T FindByID(int id);
        IEnumerable<T> FindAll();
    }
}